#!/usr/bin/make -f
# -*- makefile -*-

export DH_ALWAYS_EXCLUDE=CVS:.svn:.git

include /usr/share/cdbs/1/rules/debhelper.mk

binary-post-install/turtlebot-branding::


clean ::
	$(RM) debian/gconf-defaults

