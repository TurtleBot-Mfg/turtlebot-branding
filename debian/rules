#!/usr/bin/make -f
# -*- makefile -*-

export DH_ALWAYS_EXCLUDE=CVS:.svn

include /usr/share/cdbs/1/rules/debhelper.mk

binary-post-install/turtlebot-wallpapers::


clean ::
	$(RM) debian/gconf-defaults

